# Notebooks

Notebooks are visualization clients, never the source of reported statistics. Generate tidy results
and confirmatory analyses with the CLI/scripts first, then inspect them in `inspect_results.ipynb`.
No notebook cell writes back to a frozen result table.
